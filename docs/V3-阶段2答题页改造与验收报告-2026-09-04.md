# V3 阶段2 · 答题页改造 + 已交付页面审查验收报告

> 日期：2026-09-04　分支：`feature/v3-ios-ui`　commit：`4f914b4`（答题页 V3 改造）
> 依据：`docs/prototype/ui-v3-ios.html` + `docs/UI-v3-iOS风格实施方案-v1.0.md`（§6 验收清单）
> 原则：只替换表现层，判题 / FSRS / 存档状态机零改动

---

## 一、本轮交付：答题页 V3 改造（已 commit `4f914b4`）

| 模块 | 改动 | 设计依据 |
|---|---|---|
| 顶栏 `_V3PracticeAppBar` | 替代 GlassAppBar：模式标签（headline）+ 计时 + 答题卡入口 + 底部 3px 主色进度条（300ms easeOutCubic） | V3 §3.4 顶栏 / §2.3 进度 |
| 题干 | `IOSTypography.title1`（28pt）+ 行高 1.6 | §2.2 字号 |
| 题型徽章 | `IOSRadius.tag` 圆角，语义色保留 | §2.3 |
| 选项 `_OptionTile` | 纯白卡（`colors.card`）+ `IOSRadius.md` 圆角；选中 `primary`/正确 `success`/错误 `danger` 边框+填充；保留抖动/弹性手感 | §3.4 选项 |
| 四档评分条 | `dangerBg`/`warningBg`/`successBg` 语义色，`IOSRadius.md` | §3.4 评分 |
| 解析卡 `_ResultCard` | `IOSRadius.lg` + 左色条 + 语义色 | §3.4 解析 |
| 自由作答 | V3 白色输入框（`IOSRadius.md`、主色 focus 边框）+ 主色 pill 按钮 | §3.4 简答 |
| 语义色常量 | 移除 V2 `_kSuccess/_kError` 等；`_PracticeTimerLabel` 并入顶栏 | 令牌统一 |

**验证**：`flutter analyze` 0 issues；浏览器实机（本地 build/web + 967×883 紧凑视口）完整走通「首页 → 复习模式 → 单选判分（错选红框× / 正确绿框√）→ 解析卡「答案A」→ 四档评分 → 切题 2/3 → 再次判分」，UI 全部符合 V3 设计稿。

---

## 二、已交付 V3 页面验收结果

### 2.1 通过项（✅）

| 验收点 | 首页 | 背题 | 统计 | 答题 | 依据 |
|---|---|---|---|---|---|
| 令牌一致性（IOSColors/Spacing/Radius/Typography） | ✅ | ✅ | ✅ | ✅ | §2.1–2.3 |
| 无 Material 水波纹（NoSplash，light/dark 双主题） | ✅ | ✅ | ✅ | ✅ | ios_theme.dart L44/L294 |
| 液态玻璃仅悬浮元素（FloatingTabBar/FloatingActionBar） | ✅ | ✅ | ✅ | ✅ | §3.3 |
| 底部防遮挡 `kTContentBottomInset`(80) | ✅ | ✅ | ✅ | ✅ | 各页 ListView 底部留白 |
| 大标题 largeTitle(34pt) + 分组列表 insetGroup | ✅ | ✅ | ✅ | ✅ | §3.1 |
| 中央圆钮选中高亮（P0 修复） | ✅（真机验证） | ✅ | — | — | 阶段2 P0 |
| 判题/SRS/存档逻辑未破坏 | — | — | — | ✅ | 状态机零改动 |

### 2.2 遗留问题（⚠️ 按优先级）

| 级别 | 问题 | 位置 | 说明 / 建议 |
|---|---|---|---|
| **P2** | 科目渐变色硬编码，与 `IOSSubjectColors` 双实现 | `memorize_v3_page.dart` L152–159（6 处 `Color(0x...)`） | 背题页科目头像用自定渐变色对，首页用 `IOSSubjectColors`（系统色）。两套并存 → 统一：要么渐变进令牌，要么背题页改用 IOSSubjectColors。**留给修复会话决策** |
| **P3** | 本地静态服务字体 404（NotoSansSC 等） | 开发环境（build/web 缺字体回退资源） | 浏览器截图文字乱码仅发生在本地 file/http 静态服务；真机/打包（字体内嵌）不受影响。**非代码缺陷** |
| **P3** | 简答「作答完成」按钮：web 自动化合成点击偶发不触发 | 自动化环境局限 | 已用真实 PointerEvent 验证评分/切题正常；逻辑未改动。**建议 iPad 真机复核一次简答提交** |
| **P3** | 自动评分条点击坐标：浏览器 `click_xy` 对底部区域合成点击失效 | 自动化环境局限 | 同上，真机不受影响 |

---

## 三、与 V3 设计稿对齐度小结

- **答题页**：对齐度 **高**——顶栏、题干、选项、评分、解析、自由作答六段全部令牌化，去 Material 水波纹、内容纯白无阴影。
- **已交付四页整体**：对齐度高，仅存 memorize 页科目色双实现一处 P2 视觉不一致。

## 四、下一步建议（供修复会话参考）

1. **P2**：统一科目配色令牌（背题页 6 处渐变 → 进 `IOSSubjectColors` 或改系统色）。
2. **阶段3**：设置页 + 深色三段切换 UI（ThemePreference 已就绪，缺 UI）。
3. **阶段4**：动效一致性（AppAnim → IOSAnimations 迁移）+ 多端断点复核。
4. **验收基线**：`flutter analyze` 0 + 浏览器实机 + iPad 真机（简答提交、深色、全局返回）。
