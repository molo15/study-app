# 设计理念落位 · 差距审查报告

> 审查日期：2026-08-31
> 依据：`docs/设计理念.md`（v1.0，产品层 + 七环节 + 元理念）
> 方法：对照"环节设计核心"逐项核查代码、题库包、文档、git 现状

---

## 一、已达标项（设计理念已完整落位）

| 环节 | 现状证据 | 状态 |
|---|---|---|
| 内容层·出题 | 5 科 v0.14.0 共 4509 题；解析黑名单+字母一致性校验全绿；知识点直问直答 | ✅ |
| 结构层·知识点框架 | manifest.knowledge 79/103/94/177/110 节点，summary 全有实质（avg 63 字）；overviews 全章节覆盖 | ✅ |
| 记忆层·背题 | 背题存档 v11 已实现（跨会话+重新开始），不占 FSRS/错题本 | ✅ |
| 评估层·模拟卷 | 综合卷随机组卷、现汉古汉为主、无论述、全简答 | ✅ |
| 交互层·UI | 动效修复完成、主题开关、审题标记默认关 | ✅ |
| 工程层·版本 | formatVersion=4、App 自动选最高版本、release keystore 已配置、DB v9 增量迁移 | ✅ |
| 质量层·闭环 | P0/P1/P2 分级、打包防线、全量扫描、复核 | ✅ |

---

## 二、发现的问题（按严重度）

### 🔴 P0（1 项）—— 文档失准，会误导后续开发

**README.md 与实际严重脱节**（工程层理念"数据可校验、可回退"要求文档与现状一致）：
- README 仍写 `assets/banks` 是 **v0.11.0**（实际 v0.14.0）
- README 写测试 **52 个**（实际 67 个用例）
- README 技术链写 `pack_v4.py` 打包，实际流水线已是 `pack_v013.py + mc_expand_*`
- README 写首页"倒计时/今日任务"，实际学习目标/每日目标已移除
- README 目录里 `tools/seed-builder/pipeline` 描述过时

> 影响：任何接手者（含未来 AI）按 README 会走错流程。**必须立即修订。**

### 🟡 P1（2 项）—— 工程卫生 / 设计一致性

**1. 一次性审查脚本散落在 `tools/seed-builder/` 根目录未归档**
- git 未跟踪 `??` 的脚本 20+ 个：`check_*.py`、`audit_*.py`、`clean_know_tail*.py`、`fix_*.py`、`patch_*.py`、`verify_*.py` 等
- 项目约定：一次性脚本应归档到 `tools/seed-builder/archive/`（265 个已归档），根目录只留可复用脚本
- 现状：根目录污染，`pipeline/README.md` 的可复用清单被稀释
- **处理**：审查确认可复用 → 移入 `pipeline/`；一次性 → 移入 `archive/`；调试用临时 → 删除

**2. 备份目录未进 gitignore**
- `app/assets/banks_bak_v013/`、`app/assets/banks_bak_v014_pre_fix/` 出现在 git `??` 中
- 备份是临时回退手段，不应入库（体积大、会随版本累积）
- **处理**：加入 `.gitignore`，保留本地但不入库

### 🟢 P2（3 项）—— 设计理念与实现的表述差异

**1. `docs/设计理念.md` 原 2.3 节与背题实现矛盾（已修正）**
- 原写"知识点卡不跨会话记状态"，但实现是"跨会话存档 + 重新开始"
- 已修正为与 v11 实现一致（存档只记掌握/未掌握，不建 FSRS）

**2. 跨科知识点重叠未在理念层明确**
- 现代汉语/古代汉语同考六书、现代/当代同考梁实秋等，当前是"设计内保留"
- 理念文档 2.2 未写明这一策略 → 建议补充"跨科重叠允许，按科目优先级处理"

**3. 考试倒计时"象征性"缺理念表述**
- 实现是"距考试还有 N 天"象征展示，无计划倒排
- 理念文档 3（元理念）"自由留给用户"隐含了它，但可显式化

---

## 三、建议修复顺序

1. **修订 README.md**（对齐 v0.14.0 / 67 测试 / 新流水线 / 移除学习目标）
2. **清理 tools 脚本 + gitignore 备份目录 + git 提交**（一次性的归档或删，可复用的入 pipeline）
3. **设计理念文档补充**：跨科重叠策略、倒计时象征性（可选，理念层面一句话）

---

## 四、结论

**核心理念 95% 已落位**，无功能性缺陷；本次审查暴露的主要是**文档与工程卫生**问题（1 项 P0 + 2 项 P1），不影响发行，但影响后续开发和交接。建议在下一轮迭代开始时一并清理。
